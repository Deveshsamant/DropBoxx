<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Nocturne dark bg (the default theme); Compose repaints immediately, this only covers the first frame. -->
    <style name="Theme.DropNest" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#161826</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
